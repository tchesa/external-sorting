#ifndef HEAPSORT_H
#define HEAPSORT_H

#include "constantes.h"

void heapsort (registro v[], short ativas[], int from[], int n, short modo, int*, int*);
void heapsort_marcados (registro v[], short m[], int n, short modo, int*, int*);
#endif
